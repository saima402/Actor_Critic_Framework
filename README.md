# Actor_Critic_Framework
An Actor–Critic framework for iterative lexical ambiguity detection and query rewriting using LLMs. The actor LLM identifies ambiguous terms in the input query and a confidence score. Critic LLM transforms the ambiguous query without semantic drift. Queries of low confidence rewrite iteratively.
